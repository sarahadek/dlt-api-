[project]
name = "dlt-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dlt[workspace]>=1.22.2",
    "marimo>=0.20.2",
]
